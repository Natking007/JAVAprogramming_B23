package Day20_loops;

public class FrequencyofWords {
    public static void main(String[] args) {
        String sentence = "Java Java";
        //                 012345678
        //"Java"; how do we get the first four characters
        // we create substrings until there is no more characters to single out
       /*
        String str = sentence.substring(0,4);//"Java"
        String str2 = sentence.substring(1,5);//"ava
        String str3 = sentence.substring(2,6);//"va J"
        String str4 = sentence.substring(3,7);//"a Ja"

        */
        for(int i = 0; i <sentence.length();  i++){ //i:012345678

        }
    }
}
